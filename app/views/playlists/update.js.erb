$('#playlist-title').html('<%= link_to(@playlist.title, user_playlist_path(@playlist.user, @playlist.id)) %>')
$('.playlist_title').attr('value', '<%= @playlist.title %>')
$.ctNotify("'pList options updated", "message")
$.fallr('hide');